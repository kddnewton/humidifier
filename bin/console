#!/usr/bin/env ruby

require 'bundler/setup'
require 'humidifier'
require 'aws-sdk'

security_group = Humidifier::EC2::SecurityGroup.new(group_description: 'Test')
@stack = Humidifier::Stack.new(name: 'Thingy', resources: { 'SecurityGroup' => security_group })

require 'irb'
IRB.start
